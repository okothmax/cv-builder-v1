from django.apps import AppConfig


class TeacherpageConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'teacherpage'
    verbose_name = 'Teacher Records'  # This is the human-readable name for the admin.

