from django.apps import AppConfig


class QrcodesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'qrcodes'
    verbose_name = 'Student QR Codes'  # This is the human-readable name for the admin.
