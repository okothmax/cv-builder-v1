from django.apps import AppConfig


class AttendancepageConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'attendancepage'
    verbose_name = 'Attendance Records'  # This is the human-readable name for the admin.

